#pragma once

namespace sangi {

	// Check if a character is a alphabet character
	bool IsAlpha(char c);

	// Check if a character is a space character
	bool IsSpace(char ch);

	// Check if a character is a digit character
	bool IsDigit(char ch);

} // namespace sangi